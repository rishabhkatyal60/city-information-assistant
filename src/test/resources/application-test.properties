openweather.api.key=test-api-key-12345
openweather.base.url=http://localhost:8089
openweather.timeout=5000

# Wikipedia API configuration for testing
wikipedia.base.url=http://localhost:8089
wikipedia.timeout=5000

# Time API configuration for testing
time.api.base.url=http://localhost:8089
time.api.timeout=5000

# Logging configuration for tests
logging.level.com.example.cityassistant=DEBUG
logging.level.org.springframework.web.client=DEBUG
logging.level.wiremock=INFO

# Test database configuration (if needed)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.hibernate.ddl-auto=create-drop

# Jackson configuration for tests
spring.jackson.serialization.write-dates-as-timestamps=false

---

# src/test/resources/application.yml
# Alternative YAML configuration for tests

spring:
profiles:
active: test

jackson:
serialization:
write-dates-as-timestamps: false
deserialization:
fail-on-unknown-properties: false

server:
port: 0

management:
endpoints:
web:
exposure:
include: health,info,metrics
endpoint:
health:
show-details: always

# External API configurations
openweather:
api:
key: test-api-key-12345
base:
url: http://localhost:8089
timeout: 5000

wikipedia:
base:
url: http://localhost:8089
timeout: 5000

time:
api:
base:
url: http://localhost:8089
timeout: 5000

logging:
level:
com.example.cityassistant: DEBUG
org.springframework.web.client: DEBUG
wiremock: INFO